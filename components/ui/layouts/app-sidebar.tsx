/**
 * Re-export InternalSidebar for backward compatibility with existing imports.
 * See InternalSidebar.tsx for the canonical implementation.
 */
export { InternalSidebar, AppSidebar } from "./InternalSidebar";
