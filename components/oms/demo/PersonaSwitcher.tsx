"use client";

import * as React from "react";

/**
 * @deprecated RETIRED per docs/PORTAL-SEPARATION-AND-USERS.md Part 5.
 *
 * Real user authentication via /login with seeded credentials (Demo@2026!)
 * replaces fake cookie-injected session switching.
 */
export function PersonaSwitcher() {
  return null;
}
